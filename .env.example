# Directus (только сервер — НЕ добавляй VITE_ префикс)
DIRECTUS_URL=http://localhost:8055
DIRECTUS_TOKEN=your_token_here

# Express сервер
PORT=5000

# CORS — адрес фронтенда
CLIENT_URL=http://localhost:5173

# Vite фронтенд — URL Directus для построения ссылок на файлы
VITE_DIRECTUS_URL=http://localhost:8055

# Vite фронтенд — URL Express сервера
VITE_API_URL=http://localhost:5000
